/* PLACEHOLDER — replace with the real nayuki/QR-Code-generator minified
 * build before the v0.12.0 release tag. See vendor/README.md for the
 * pinned-fetch procedure + SHA recording.
 *
 * Until the real file is dropped, `window.qrcodegen` stays undefined and
 * onboard.js falls back to a "QR unavailable — use a button or copy the
 * URL" note. The page is fully functional for deep-link import without
 * the QR; the QR is camera-scan convenience only.
 *
 * Intentionally defines nothing — the graceful-degradation path in
 * onboard.js (typeof window.qrcodegen === "undefined") handles it.
 */
